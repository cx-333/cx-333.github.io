# 本地修改信息说明书

主页：<https://cx-333.github.io/>  
仓库：<https://github.com/cx-333/cx-333.github.io>  
本地目录：`D:\myprofile\phd-resume`

## 1. 启动本地预览

安装 Git、Node.js 22.12 或更高的 22.x 版本，并使用文本编辑器（如 VS Code）打开项目。在 PowerShell 中执行：

```powershell
cd D:\myprofile\phd-resume
npm.cmd ci
npm.cmd run dev
```

打开终端显示的地址（通常是 `http://localhost:5173/`），保存文件后浏览器会自动更新。结束时按 Ctrl+C。使用 `npm.cmd` 可避免 PowerShell 对 `npm.ps1` 的执行策略限制。

新电脑先获取源码：

```powershell
git clone https://github.com/cx-333/cx-333.github.io.git
cd cx-333.github.io
npm.cmd ci
npm.cmd run dev
```

## 2. 修改位置速查

大部分个人信息在 `src/App.jsx` 顶部的 `const resumeData = { ... }` 中。

| 内容 | 字段或文件 | 说明 |
| --- | --- | --- |
| 姓名、身份、所在地 | name、title、location | 修改引号内文字 |
| 联系邮箱 | email | 只填邮箱，页面自动生成邮件链接 |
| 代码主页 | github | 填完整 GitHub/Gitee 地址 |
| 学术档案 | scholar | 填 Google Scholar 地址 |
| 简历 PDF | cv | 例如 /cv.pdf |
| 自我介绍 | summary | 替换为研究概述 |
| 研究兴趣 | interests | 字符串列表 |
| 成果数量 | metrics | label 为标题，value 为数量，不会自动统计 |
| 教育背景 | education | 学位、学校、时间和说明 |
| 技能 | skills | group 为分组，items 为技能列表 |
| 论文 | publications | 标题、会议、年份、状态、标签、摘要、链接 |
| 项目 | projects | 名称、时间、标签、介绍、成果 |
| 经历 | experiences | 职务、单位、时间、工作内容 |
| 奖项 | awards | 字符串列表 |
| 浏览器标题、搜索简介 | index.html | title 和 description 的 content |
| 颜色、卡片、布局、字号 | src/App.css | 浅色变量在 :root，深色变量在 .app.dark |
| 全局字体 | src/index.css | body 中的 font-family |
| 浏览器图标 | public/favicon.svg | 替换同名文件 |

部分文字直接写在 `src/App.jsx` 下半部分：导航标志 `CS`、`Interactive Resume`、`PhD Candidate · Computer Science`、`Research Focus` 下的概述，以及底部联系区的合作意向。用编辑器搜索这些文字即可修改。

当前姓名、学校、论文、数量、邮箱等是示例资料；`#` 是占位链接，不会打开真实内容，请逐项替换。

## 3. 修改资料与简历

只替换对应字段的值，保留周围逗号和括号，例如：

```javascript
name: "你的真实姓名",
title: "计算机科学博士研究生",
location: "北京 / 某某大学",
email: "你的邮箱",
github: "https://github.com/cx-333",
scholar: "https://scholar.google.com/citations?user=你的ID",
cv: "/cv.pdf",
```

把简历复制到 `public/cv.pdf`，将 `cv` 改为 `/cv.pdf`。浏览器可能先打开 PDF，再由用户保存。更新简历时替换同名文件即可。

其他附件也放在 `public/`。例如 `public/files/paper.pdf` 对应 `/files/paper.pdf`。链接中不要写 `public/` 或电脑磁盘路径。文件名大小写必须一致，建议用英文、数字和连字符。

## 4. 新增论文、项目和经历

在 `publications: [ ... ]` 中复制一个完整条目再修改，例如：

```javascript
{
  title: "论文标题",
  venue: "会议或期刊名称",
  year: "2026",
  status: "Published",
  tags: ["Machine Learning", "AI Systems"],
  description: "用一两句话介绍论文贡献。",
  link: "https://doi.org/替换为真实DOI",
},
```

按数组顺序显示，可将最新论文放最前面。标签筛选自动读取 tags；搜索覆盖标题、会议、年份、状态、说明和标签。每篇论文标题应唯一。

项目、教育和经历同样复制同类条目；删除时删除完整条目及分隔逗号。兴趣和奖项直接增删字符串。保留英文引号、英文逗号及配对的 `{}`、`[]`；文字内部可使用中文引号“”。

## 5. 检查与上线

如果换过电脑或其他人更新过仓库，先在工作区无未提交修改时执行 `git pull --ff-only origin main`，再编辑。

```powershell
npm.cmd run lint
npm.cmd run build
npm.cmd run preview
```

打开终端给出的预览地址（通常为 `http://localhost:4173/`），检查姓名、邮箱、CV、论文链接、搜索筛选、深浅色切换和窄屏布局。不要直接双击 dist/index.html。

确认后停止预览并提交：

```powershell
git status
git add .
git commit -m "更新个人信息和论文列表"
git push origin main
```

`git add .` 会加入所有未忽略的项目改动，提交前检查文件列表。node_modules 和 dist 已忽略，无需上传。

打开 [Actions](https://github.com/cx-333/cx-333.github.io/actions)，等待 **Deploy GitHub Pages** 和后续 Pages 发布任务成功，再访问主页。推送成功不等于上线完成。也可以在工作流页面选择 **Run workflow** 手动重发。

当前本地 origin 指向 GitHub，通过已配置的 SSH 身份推送；原 Gitee 远程另存为 gitee。新电脑通过 HTTPS 克隆时按 Git 提示登录 GitHub，不能用 GitHub 登录密码作为 Git 密码。

## 6. 部署结构和旧版本

- main：源码、依赖锁文件、README、说明书和部署配置。
- gh-pages：构建出的静态网页，自动清理旧文件并只保留最新发布提交。
- dist：本地构建结果，每次构建重新生成，不应直接编辑。
- Pages 设置：Settings → Pages → Deploy from a branch → gh-pages → / (root)。
- vite.config.js 的 base 保持相对路径；本仓库直接发布到用户名根域名。

本次迁移用当前项目替换旧源码和旧发布分支，不合并原 Hugo 网站。之后正常推送 main 即可，无需强推。旧提交从分支历史中移除，不代表 GitHub 后台立即物理删除所有历史对象或缓存。

## 7. 常见问题

| 问题 | 解决方式 |
| --- | --- |
| 仍是旧内容 | 确认 Actions 全部成功后按 Ctrl+F5；Pages/CDN 更新可能需要几分钟 |
| 部署失败 | 打开红色任务查看失败步骤，本地重跑 lint 和 build |
| 网页空白 | 检查构建及资源路径，发布目标应是 dist 内容 |
| PDF/图片 404 | 确认 public 中的文件已提交，路径大小写一致 |
| 链接无效 | 将 # 占位符改成真实网址 |
| Node 版本不支持 | 使用 Node.js 22.12 或更高的 22.x，重新 npm.cmd ci |
| 推送被拒绝 | 检查 git remote -v 和登录权限；先协调同步远程修改，不要直接强推 |
| 撤销已发布改动 | git log --oneline 找到提交，git revert 提交号，再 git push origin main |

参考：[GitHub Pages 发布源](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)、[发布 Action 文档](https://github.com/peaceiris/actions-gh-pages)。
